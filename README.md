# API Scanner

A lightweight API scanner that discovers endpoints, runs common security checks (CORS, security headers, SQLi/XSS reflection checks, sensitive data leakage), and produces JSON and HTML reports including detailed findings and remediation guidance.

See scanner_cli.py for usage.
